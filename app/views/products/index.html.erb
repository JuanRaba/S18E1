<h1>Products</h1>

<table>
<% @products.each do |u| %>
  <tr>
    <td><%=u.name%></td>
    <td><%=u.description%></td>
    <td>$<%=u.price%></td>
    <td><%=u.seller.try(:email)%></td>

  </tr>
<% end%>
</table>